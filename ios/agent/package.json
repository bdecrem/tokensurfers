{
  "name": "tokensurfers-agent",
  "private": true,
  "type": "module",
  "description": "Token Surfers' coding agent: Claude Code (Agent SDK) run headless per app workspace, with a long-poll event feed for the phone.",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.282"
  }
}
